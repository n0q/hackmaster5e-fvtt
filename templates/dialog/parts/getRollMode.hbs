<div class="form-group">
    <label>{{localize 'HM.dialog.rollMode'}}</label>    
        <select id='rollMode' class='bind' data-prop='rollMode'>
            {{#select rollMode}}{{#each rollModes as |label mode|}}
            <option value="{{mode}}">{{localize label}}</option>
            {{/each}}{{/select}}
        </select>
    </div>
</div>
