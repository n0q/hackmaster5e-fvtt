<form class="{{cssClass}}" autocomplete="off">
    <div class="item-form">
        {{#with data.data}}
        <div class="sidepanel">
            <div class="options-block">
                <div class="options-row">
                    <label class="resource-label">{{localize "HM.hp"}}:</label>
                    <input type="number" name="data.hp.value" value="{{hp.value}}" data-dtype="Number"/>
                </div>
                <div class="options-row">
                    <label class="resource-label">{{localize "HM.duration"}}:</label>
                    <input type="number" name="data.timer.value" value="{{timer.value}}" data-dtype="Number"/>
                </div>
                <div class="options-row">
                    <label class="resource-label">{{localize "HM.treated"}}:</label>
                    <input type="checkbox" name="data.treated.checked" {{checked treated.checked}}/>
                </div>
            </div>
        </div>
        {{/with}}
    </div>
</form>
