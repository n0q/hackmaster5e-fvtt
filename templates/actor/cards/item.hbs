<li class='card hmstyle' data-item-id='{{_id}}'>
    <summary>
        <header>
            <label for='{{name}}'>
                <div class='item-image'><img src='{{img}}' title='{{name}}' width='28' height='28'/></div>
                <h4 class='item-name'>{{name}}</h4>
            </label>
        </header>
        <section>
            <div class='card-controls'>
                <a class='item-control item-edit' title='Edit Item'><i class='fas fa-edit'></i></a>
                <a class='item-control item-delete' title='Delete Item'><i class='fas fa-trash'></i></a>
            </div>
            <div class='inventory-details'>
                <span>
                    {{localize (findConfigValue 'itemTypes' type)}}
                </span>
                <div class='item-control item-state location {{#unless (eq data.state 3)}}button{{/unless}}'>
                    {{localize (findConfigValue 'thingState' data.state)}}
                </div>
                {{#unless data.innate}}
                <div>{{#if (eq type 'item')}}{{data.qty}}x{{/if}}</div>
                <span>{{numberFormat data.weight decimals=2}} lbs.</span>
                {{/unless}}
            </div>
        </section>
    </summary>
</li>
