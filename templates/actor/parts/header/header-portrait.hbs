<span class="flexrow">
    <img class="profile-img" src="{{actor.img}}" data-edit="img" title="{{actor.name}}"/>
    <div>
        <input name="name" type="text" value="{{actor.name}}" placeholder="Name"/>
        <div>
            {{#if actor.race.name}}{{actor.race.name}}{{/if}}
            {{#if actor.curr_class}}{{actor.curr_class.name}}-{{actor.curr_class.data._ord}}{{/if}}
        </div>
    </div>
</span>
