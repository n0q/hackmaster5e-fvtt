{{#unless (eq formulaType 'opposed')}}
    {{#unless (eq dc 'auto')}}
        {{localize 'HM.target'}}:
        <b>{{localize (lookup (findConfigObj 'skillLevel') dc)}}</b>
        <br>
    {{/unless}}
    <b>{{specialRow}}</b>
{{/unless}}
