{{localize 'HM.skill'}}:
<b>{{specname}}</b><br>
{{localize 'HM.skillmastery'}}:
<b>{{level}}
({{localize (lookup (findConfigObj 'skillMastery') mastery)}})</b><br>
{{#unless (eq formulaType 'opposed')}}
    {{#unless (eq dc (HMCONST 'SKILL.DIFF.AUTO'))}}
        {{localize 'HM.target'}}:
        <b>{{localize (lookup (findConfigObj 'skillLevel') dc)}}</b>
        <br>
    {{/unless}}
    <hr>
    <b>{{specialRow}}</b>
{{/unless}}
